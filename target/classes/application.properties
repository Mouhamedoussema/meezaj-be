spring.application.name=meezaj-backend
server.port=8080

# PostgreSQL

spring.datasource.url=jdbc:postgresql://ep-broad-waterfall-altrov1c.c-3.eu-central-1.aws.neon.tech/neondb?sslmode=require
spring.datasource.username=neondb_owner
spring.datasource.password=npg_g6yFIZw3iaXv

spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT
app.jwt.secret=meezaj_super_secret_jwt_key_change_in_production_2024
app.jwt.expiration=86400000

# CORS
app.cors.allowed-origins=http://localhost:4200

# File upload
app.upload.dir=${user.dir}/uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
